var modal = $('#app_modal');
 modal.on('show.bs.modal', function (event) {
 modal.find('.modal-body').html('<%= escape_javascript(render(:partial => "locations_list")) %>');
 modal.find('.modal-title').html("Locations");   
});

modal.modal('show');