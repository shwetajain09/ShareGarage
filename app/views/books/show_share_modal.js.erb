modal = $('#app_modal');
modal.find('.modal-title').text('Add Location');
modal.find('.modal-footer').remove();
modal.find('.modal-body').html("<%= escape_javascript(render(:partial => 'add_location_form',:locals=>{:isbn => params[:isbn]})) %>");

$('#app_modal').modal('show');