modal = $('#app_modal');
modal.on('show.bs.modal', function (event) {
	modal.find('.modal-title').text('Book owners');
	modal.find('.modal-body').html("<%= escape_javascript(render(:partial => 'providers_listing')) %>");
	modal.find('.modal-footer').html("<span><i>*Get Socialise by meeting the book owner at a conveneint and friendly place. Make sure you exchange your BookCoin and give owner's review</i> </span><button type='button' class='btn btn-default' data-dismiss='modal'>Close</button>");
			
})

$('#app_modal').modal('show');
